/**
 * @param {string} s
 * @return {string}
 */
// var decodeString = function (s) {
//   let firstIndex = s.indexOf("["),
//     lastIndex = s.lastIndexOf("]");
//   let head = s.slice(0, firstIndex),
//     middle = s.slice(firstIndex + 1, lastIndex),
//     end = s.slice(lastIndex + 1);
//   let headString = "",
//     headNum = "";
//   (middleString = ""), (endString = end.split("").join(""));
//   for (let i = 0; i < head.length; i++) {
//     if (head[i] >= 97) {
//       headString += head[i];
//     } else {
//       headNum += head[i];
//     }
//   }
//   if (!middle.includes("["))
//     middleString = middle
//       .split("")
//       .join("")
//       .repeat(Number(headNum) || 1);

//   for(let i = 0; i < middle.length;i++){

//   }
// };
